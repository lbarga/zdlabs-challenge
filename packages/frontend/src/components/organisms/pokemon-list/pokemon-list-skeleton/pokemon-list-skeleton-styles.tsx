import { Box } from "@mui/material";
import styled from "styled-components";

export const SearchPageSkeletonCardsContainer = styled(Box)`
  display: flex;
  gap: 16px;
  flex-wrap: wrap;
  max-width: 70vw;
  justify-content: center;
  align-items: center;
`;
